# Copy this file to terraform.tfvars and adjust as needed.
# No secret values belong here - only non-sensitive configuration.

aws_region   = "ap-northeast-1"
project_name = "cloudfront-signed"

# Leave unset to auto-generate a globally unique name from the account ID.
# bucket_name = null

public_key_path = "../keys/public_key.pem"
secret_name     = "cloudfront-signed/private-key"

cloudfront_price_class = "PriceClass_100"

# Origins allowed to PUT directly to S3 via presigned URLs (cmd/upload-server verification UI).
upload_cors_allowed_origins = ["http://localhost:8080"]

tags = {
  Project   = "cloudfront-signed"
  ManagedBy = "terraform"
}
